#include <bits/stdc++.h>
using namespace std;

int main()
{
    int q;
    cin >> q;
    cin.ignore();
    // getchar();

    while (q--)
    {
        string name;
        getline(cin, name);

        cout << name << "\n";
    }


    return 0;
}