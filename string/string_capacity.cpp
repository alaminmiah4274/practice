#include <bits/stdc++.h>
using namespace std;


int main()
{
    string name = "Al Amin Miah";

    // cout << name.size() << "\n";
    // cout << name.max_size() << "\n";
    // cout << name.capacity() << "\n";

    // name.clear();

    // if (name.empty()) cout << "empty" << "\n";
    // else cout << "not empty" << "\n";


    // name.resize(5);
    name.resize(20, 'B');

    cout << name << "\n";

    return 0;
}